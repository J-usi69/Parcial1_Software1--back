spring:
  data:
    mongodb:
      # En producción tomará la URL de la variable de entorno MONGO_URI
      uri: ${MONGO_URI}

  ai:
    vertex:
      ai:
        gemini:
          project-id: project-da37a9fc-9c9e-4d37-822
          location: us-central1
          chat:
            options:
              model: gemini-2.5-flash-lite
              temperature: 0.1

server:
  port: ${PORT:8080}

logging:
  level:
    root: INFO
    com.workflow: INFO
